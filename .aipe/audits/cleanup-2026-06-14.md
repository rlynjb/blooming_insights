# Cleanup audit — 2026-06-14

> **Verdict-first.** Zero code has been touched since the 2026-06-02 audit — every prior fix-now item is still live, and the through-line of THIS audit is "the afternoon list never got done." That's the most important sentence in this file. The audit also widens one prior finding: the NDJSON parser is now duplicated across **four** sites (page.tsx main effect + page.tsx capture path + useInvestigation.ts + StreamingResponse.tsx), not two. Everything else moves in lockstep with 06-02 — the architectural debt is the same shape, the structural debt cluster is the same cluster, and the eval gap is still `cannot-clean`. The structural lens is the only place the audit grew: re-rating the NDJSON parser duplication from accept-territory to fix-later (and a new finding) is the only meaningful delta.
>
> **Triage shape:** 7 fix-now (3 structural, 4 architectural, 0 dsa, 0 test) · 10 fix-later · 5 accept · 1 cannot-clean. Total: 23 findings. The fix-now count is unchanged because nothing was done; the fix-later count is +1 because one accept (lucide-react) resolved and one new structural finding promoted in (NDJSON parser now N=4). The eval-gap `cannot-clean` survives unchanged.

---

## Top 3 fix-now (the afternoon list — STILL)

The exact same three items called out 12 days ago. None landed. Do these this week — they are still the highest leverage-to-effort ratio in the entire pile, all S effort, all high severity:

1. **Session-key the insights `Map`** — `lib/state/insights.ts:4`, ~30 LOC. The only correctness bug at any current scale; user-visible above ~10 concurrent users.
2. **Add `res.usage` logging at the four Anthropic call sites** — `lib/agents/base.ts:102`, `lib/agents/diagnostic.ts:97`, `lib/agents/recommendation.ts:96`, `lib/agents/intent.ts:18`, ~5 LOC. Cheapest fix in the codebase for the most consequential blind spot.
3. **Strip `e.stack` from the four error-response JSON bodies** — `app/api/mcp/call/route.ts:18`, `app/api/mcp/tools/route.ts:19`, `app/api/mcp/tools/check/route.ts:23`, `app/api/mcp/capture/route.ts:55`, ~4 LOC. Info-disclosure across the route → browser trust boundary.

If only one slot opens this week, it's #1 — the session-Map bleed is the only finding in the audit that is *actively wrong* under the demo-scale load the recon documents.

---

## fix-now (7 items)

### 1. Global-Map insight wipe under concurrent users

**Lens:** architectural
**Severity:** high — actively wrong above ~10 concurrent users on a warm Vercel instance; user-visible as feed flicker / investigation 404. Re-confirmed: `lib/state/insights.ts:4` still uses an un-sessioned `Map<id, Insight>` and `putInsights` still calls `insights.clear()` globally on every briefing.
**Effort:** S (under an hour, one file + 3 call sites)
**Decision:** fix-now
**Refactor-shape:** Change `lib/state/insights.ts:4` from `Map<id, Insight>` to `Map<sessionId, Map<id, Insight>>`; thread `sessionId` (already available via `getOrCreateSessionId` in `lib/mcp/session.ts:16`) through `putInsights` / `getInsight` / `listInsights` / `getAnomaly` and the call sites in `app/api/briefing/route.ts:243` and `app/api/agent/route.ts:48-50`. Per-session sub-map gets `.clear()` on rebriefing, not the global Map. Behaviour preserved per session; the cross-session bleed is the bug being removed.

> Cross-ref: `study-system-design/audit.md` Top-3 #1 + Ceiling 1; `recon-2026-06-02.md` "the critical correctness bug — named but unfixed." Cleanup audit 2026-06-02 finding #1 — **still live.**

---

### 2. `res.usage` dropped on the floor at every Anthropic call

**Lens:** architectural (observability seam)
**Severity:** high — every prompt edit and model swap ships blind; the dominant cost line (`synthesize()`, finding #9) cannot be sized. Re-confirmed: no `console.log({ usage: res.usage })` exists in `lib/agents/` or `lib/mcp/`.
**Effort:** S (5 lines total)
**Decision:** fix-now
**Refactor-shape:** Add `console.log({ site, usage: res.usage, sessionId })` after `anthropic.messages.create` at the four call sites: `lib/agents/base.ts:102`, `lib/agents/diagnostic.ts:97`, `lib/agents/recommendation.ts:96`, `lib/agents/intent.ts:18`. No control-flow change, no API change, no test impact. Behaviour identical; one read of an existing field.

> Cross-ref: `study-performance-engineering/audit.md` Top-3 #1 — "cheapest fix in the codebase for the most consequential blind spot." Cleanup audit 2026-06-02 finding #2 — **still live.**

---

### 3. Four routes leak `e.stack` in JSON error bodies

**Lens:** structural (consistency drift) + architectural (trust boundary)
**Severity:** high — info disclosure across the route → browser trust boundary; reveals file paths, library entry points, function names to any caller. Re-confirmed: all four routes still emit `${e.message}\n${e.stack ?? ''}` verbatim.
**Effort:** S (one line per route, 4 routes)
**Decision:** fix-now
**Refactor-shape:** In `app/api/mcp/call/route.ts:18`, `app/api/mcp/tools/route.ts:19`, `app/api/mcp/tools/check/route.ts:23`, `app/api/mcp/capture/route.ts:55`, change `e.message + '\n' + (e.stack ?? '')` to `e.message`. Keep `console.error(e)` so the operator still sees the stack in Vercel logs. The streaming routes (`/api/briefing`, `/api/agent`) already use this safe shape — the inconsistency is the finding.

> Cross-ref: `study-security/audit.md` Top-3 #2. Cleanup audit 2026-06-02 finding #3 — **still live.**

---

### 4. `POST /api/mcp/call` accepts any tool name (no allowlist)

**Lens:** architectural (capability gating)
**Severity:** high today by recon rule (gated only by session auth); becomes critical the moment Bloomreach adds a write tool. Re-confirmed: `app/api/mcp/call/route.ts:13` still passes `name` straight to `conn.mcp.callTool` with zero gating.
**Effort:** S (one Set + one guard, ~10 LOC)
**Decision:** fix-now
**Refactor-shape:** At the top of `app/api/mcp/call/route.ts`, build `const ALL_KNOWN = new Set([...monitoringTools, ...diagnosticTools, ...recommendationTools, ...bootstrapTools])` (imports from `lib/mcp/tools.ts`); after `req.json()`, `if (typeof name !== 'string' || !ALL_KNOWN.has(name)) return NextResponse.json({ error: 'tool not allowed' }, { status: 403 })`. Behaviour preserved for the `/debug` UI that exercises this route (it only calls known tools); the new rejection is the *added* safety, not a behaviour change to existing callers.

> Cross-ref: `study-security/audit.md` Top-3 #1, finding A7/B6. Also see `study-security/05-open-tool-surface-gap.md`. Cleanup audit 2026-06-02 finding #4 — **still live.**

---

### 5. No per-tool timeout on MCP calls

**Lens:** architectural (failure containment seam)
**Severity:** high — a Bloomreach connection that hangs forever burns the entire 300s route budget on one stuck call; no signal, no recovery. Re-confirmed: `lib/mcp/transport.ts:47-59` and `lib/mcp/client.ts:148-163` have no `AbortSignal`, no `Promise.race`, no timeout primitive.
**Effort:** S (~15 LOC inside `SdkTransport.callTool`)
**Decision:** fix-now
**Refactor-shape:** In `lib/mcp/transport.ts:47-59`, wrap the `this.client.callTool({ name, arguments: args })` call in `Promise.race([..., timeoutPromise(30_000)])` or pass an `AbortSignal` from `AbortSignal.timeout(30_000)` into the SDK if the SDK supports it. Throw `McpToolError(name, 'timeout after 30s')` on timeout. No public API change; `McpClient` already handles thrown errors via the existing retry ladder. The `30_000` ceiling sits in the same shape as `retryCeilingMs: 20_000` in `lib/mcp/client.ts:94` — the rationale lives next to that comment.

> Cross-ref: `study-system-design/audit.md` HIGH finding #2; `recon-2026-06-02.md` "no per-tool timeout." Cleanup audit 2026-06-02 finding #5 — **still live.**

---

### 6. No phase-timing observability on the 300s budget

**Lens:** architectural (observability)
**Severity:** high — the day a request hits the 300s wall in production, there is no signal which phase ate the time (schema bootstrap / coverage gate / monitoring / diagnostic / recommendation). Re-confirmed: `app/api/briefing/route.ts` and `app/api/agent/route.ts` have zero `performance.now()` or duration spans in their handlers; only `tool_call_end.durationMs` exists, which is the *per-call* layer below this.
**Effort:** S (~20 LOC across two routes)
**Decision:** fix-now
**Refactor-shape:** In `app/api/briefing/route.ts` and `app/api/agent/route.ts`, wrap each major phase with `const t0 = performance.now(); … const elapsed = performance.now() - t0;` and emit `console.log({ route, phase, durationMs: elapsed, sessionId, complete })` on `done`. No API change, no NDJSON wire change (logs are server-only).

> Cross-ref: `study-system-design/audit.md` Top-3 #2; `study-performance-engineering/audit.md` "no Web Vitals / time-to-first-event metric" (the route-side half). Cleanup audit 2026-06-02 finding #6 — **still live.**

---

### 7. `e.cause` chain logged unfiltered (possible token echo)

**Lens:** structural (one-line filter; logging hygiene)
**Severity:** high if it fires (token in Vercel logs = same as a secret in logs); paired with #3 above as a cluster — both are "what gets logged where." Re-confirmed: `lib/mcp/transport.ts:24-36` captures up to 2000 bytes of the raw response body into `holder.last.body` and that body flows straight into a thrown `Error.cause`, which `lib/mcp/client.ts:errorDetail` then `JSON.stringify`s into the surfaced detail.
**Effort:** S (one regex, one wrapper function)
**Decision:** fix-now
**Refactor-shape:** Add a `redactSecrets(text: string): string` helper in `lib/mcp/transport.ts` that strips `Bearer [a-zA-Z0-9._-]+` and `"access_token":\s*"[^"]+"` patterns. Wrap the body capture in `makeCapturingFetch` at `lib/mcp/transport.ts:29` and any `console.error` that prints `err.cause` in the agent path. No control-flow change; the redaction is post-capture.

> Cross-ref: `study-security/audit.md` finding C7; `recon-2026-06-02.md` correlates with the 4-routes-leak-stack cluster. Cleanup audit 2026-06-02 finding #7 — **still live.**

---

## fix-later (10 items — track but don't act this pass)

### 8. `app/page.tsx` is 817 LOC, 8 concerns, 15 useState slots, parses NDJSON twice

**Lens:** structural (shallow module / cognitive load hotspot)
**Severity:** medium — slows future work, makes bugs more likely, but no current correctness issue. Note: re-counting at 2026-06-14, the page actually has **15** `useState` calls (one more than 06-02 reported as 14) and **2** `useEffect` calls. NDJSON parsing now appears at `app/page.tsx:181` (capture path) AND `app/page.tsx:323` (main briefing effect) — the same two-place duplication exists inside one file.
**Effort:** L (a day or more; touches one file but cascades to NDJSON parser shared with `useInvestigation.ts` and `StreamingResponse.tsx`)
**Decision:** fix-later
**Refactor-shape:** Extract `useBriefingStream(mode)`, `useReconnectPolicy()`, `useDemoCapture(insights, workspace, trace)` into `lib/hooks/`. Page collapses to ~120 LOC of layout + composition. Behaviour preserved if the existing 15 `useState` slots are mapped 1:1 into the three hooks' returned state.

**Why not fix-now:** L-effort refactors during cleanup are the canonical "behaviour-preserving slides into behaviour-changing" failure mode. This deserves its own dedicated session with the existing NDJSON tests pinned as the contract, but the suite *has no NDJSON tests* (see #15) — meaning this refactor would land without a safety net. **Block on #15 (NDJSON harness) AND #24 (NDJSON-parser extraction) first.**

> Cross-ref: `study-software-design/audit.md` red-flag #1, Top-3 #1; `study-software-design/02-shallow-module-page-component.md` for the deep walk. Cleanup audit 2026-06-02 finding #8 — **still live, slightly worse (+1 useState slot).**

---

### 9. `synthesize()` duplicated across two agents (~90 LOC removable)

**Lens:** structural (duplication) + architectural (special case sprawl)
**Severity:** medium — two copies drift independently; same shape, same recovery semantics. Re-confirmed: `lib/agents/diagnostic.ts:87-126` and `lib/agents/recommendation.ts:82-132` are byte-for-byte structurally identical, only the prompt strings and parser hook differ.
**Effort:** M (half day; modifies `runAgentLoop` signature, removes two methods, updates tests)
**Decision:** fix-later
**Refactor-shape:** Lift `synthesize()` into `runAgentLoop` (`lib/agents/base.ts:48-176`) as `parseResult: (text) => T | null` + `recoveryPrompt: (toolCalls) => string` options. Loop runs as normal, attempts parse, runs ONE tool-less recovery turn on failure. Both `synthesize()` methods delete. Existing scripted-Anthropic tests cover the path; the test seam survives.

**Why not fix-now:** medium severity, M effort — track-and-batch territory. Touches the most-tested deep module in the codebase (`runAgentLoop`); deserves its own focused session, not a side dish.

> Cross-ref: `study-software-design/audit.md` red-flag #3, Top-3 #3; `study-software-design/04-synthesize-recovery-duplication.md`. Cleanup audit 2026-06-02 finding #9 — **still live.**

---

### 10. `Insight ↔ Anomaly` field-copy lives in 3 files (silent leak)

**Lens:** structural (information leakage) + architectural (wire format)
**Severity:** medium — TypeScript catches the interface change but not the two copy sites; a new field on `Anomaly` silently drops in the `insightToAnomaly` round-trip. Re-confirmed: `app/api/agent/route.ts:29-31` and `lib/state/insights.ts:8-28` still hold the two halves.
**Effort:** M (half day; colocation refactor + round-trip test, OR wire-format change)
**Decision:** fix-later
**Refactor-shape:** Cheap version: move `insightToAnomaly` from `app/api/agent/route.ts:29-31` next to `anomalyToInsight` in `lib/state/insights.ts`; add a round-trip test asserting `insightToAnomaly(anomalyToInsight(a))` preserves every field. Deeper version (cannot-clean — see #23): change the wire format so `/api/agent` accepts only the insight id and looks up the cached `Anomaly` server-side.

**Why not fix-now:** the cheap version only patches the symptom (test catches loss after the fact). The real fix requires the wire-format change in #23. Park.

> Cross-ref: `study-software-design/audit.md` red-flag #2, Top-3 #2; `study-software-design/03-insight-anomaly-silent-leak.md`. Cleanup audit 2026-06-02 finding #10 — **still live.**

---

### 11. No circuit breaker on the McpClient retry ladder

**Lens:** architectural (failure containment)
**Severity:** medium — during a 5-minute Bloomreach outage at ~10 concurrent users, every request burns 3 retries × ~12s = ~36s before failing; budget evaporates. Fine at 1 user today.
**Effort:** M (~50 LOC; new state on the class, two test files updated)
**Decision:** fix-later
**Refactor-shape:** Add `consecutiveFailures: Map<toolName, number>` + `openUntil: Map<toolName, timestamp>` state to `McpClient`. In the retry handler at `lib/mcp/client.ts:121-132`, after 5 consecutive 5xx for a tool name, set `openUntil = now + 30_000` and fail fast (`McpToolError(name, 'circuit open')`). Close on first success. ~50 LOC, no external dep.

**Why not fix-now:** medium severity at current load. The session-Map fix (#1) is the ~10x trigger; the circuit breaker is the ~100x horizon. Track behind it.

> Cross-ref: `study-system-design/audit.md` Top-3 #3, Ceiling 2; `study-performance-engineering/audit.md` Top-3 #3. Cleanup audit 2026-06-02 finding #11 — **still live.**

---

### 12. Schema cache has no TTL

**Lens:** architectural (caching policy)
**Severity:** medium — brittle the day a customer adds a new event type and a warm instance keeps serving the stale schema for hours. Re-confirmed: `lib/mcp/schema.ts:131` still uses `let cached: WorkspaceSchema | null = null` with no expiry.
**Effort:** S (~10 LOC)
**Decision:** fix-later
**Refactor-shape:** In `lib/mcp/schema.ts:131`, change `let cached: WorkspaceSchema | null = null` to `let cached: { value: WorkspaceSchema; expiresAt: number } | null = null` with a 1-hour TTL. Optional `?force=true` query param on the bootstrap path. ~10 LOC.

**Why not fix-now:** the trigger doesn't fire today — single-tenant demo against one project. Park until multi-tenant or a customer reports stale schemas.

> Cross-ref: `study-system-design/audit.md` red-flag #4; `study-performance-engineering/audit.md` red-flag #6. Cleanup audit 2026-06-02 finding #12 — **still live.**

---

### 13. `investigations` Map grows monotonically (no LRU, no delete)

**Lens:** architectural (memory bound)
**Severity:** medium — classic Node leak pattern; instance dies before it matters today, but bites when traffic patterns change.
**Effort:** S (~20 LOC LRU cap)
**Decision:** fix-later
**Refactor-shape:** In `lib/state/investigations.ts:11`, wrap the `mem: Map<string, AgentEvent[]>` in a small LRU with `MAX_INVESTIGATIONS = 100`. On `set`, if size > cap, delete the oldest key. ~20 LOC.

**Why not fix-now:** instance recycle bounds it in production today. Add when traffic justifies it or as part of the Vercel KV migration.

> Cross-ref: `study-performance-engineering/audit.md` red-flag #5. Cleanup audit 2026-06-02 finding #13 — **still live.**

---

### 14. `AUTH_SECRET` has no strength enforcement

**Lens:** structural (input validation at app boundary)
**Severity:** medium — `AUTH_SECRET=password` is silently SHA-256'd into a 32-byte AES key with ~10 bits of underlying entropy. The `.env.example` documents `openssl rand -base64 32`; the code at `lib/mcp/auth.ts:51-60` doesn't enforce it.
**Effort:** S (one line)
**Decision:** fix-later
**Refactor-shape:** In `lib/mcp/auth.ts:51-60` (`aesKey`), add `if (secret.length < 32) throw new Error('AUTH_SECRET must be at least 32 characters')` before the SHA-256. One line.

**Why not fix-now:** strict gating could break dev environments. Park behind #1-#7 because it's a deployment-prep item, not a code-health item.

> Cross-ref: `study-security/audit.md` Top-3 #3, finding C3. Cleanup audit 2026-06-02 finding #14 — **still live.**

---

### 15. No route-level integration tests; no NDJSON-stream test

**Lens:** test
**Severity:** medium — load-bearing route (`app/api/agent/route.ts`) has zero tests; every wire between tested lib pieces is unverified. **Blocks safe execution of #8.**
**Effort:** M (extract `runInvestigation(deps)` from the route handler, then apply the existing scripted-Anthropic pattern one layer up)
**Decision:** fix-later
**Refactor-shape:** Extract `runInvestigation(deps: { anthropic, mcp, schemaProvider, sessionId, insight, … })` as a pure async function out of `app/api/agent/route.ts`; leave `GET` as a 5-line wrapper that wires deps from `connectMcp`. Add `test/api/agent.test.ts` using the scripted-Anthropic pattern from `test/agents/base.test.ts`, asserting NDJSON event order + `done` sentinel.

**Why not fix-now:** M effort, and it's *enabling* infrastructure rather than fixing a fire. Fix-soon priority because #8 cannot land safely without it.

> Cross-ref: `study-testing/audit.md` Top-3 #1; `study-testing/01-scripted-anthropic-harness.md`. Cleanup audit 2026-06-02 finding #15 — **still live.**

---

### 16. CSRF on `POST /api/mcp/reset`; GETs with side effects on `/api/briefing` and `/api/agent`

**Lens:** architectural (trust boundary)
**Severity:** medium — `SameSite=None` on the session cookie + no Origin check means a cross-origin form post logs the user out (`reset`) or burns Anthropic tokens (`briefing` / `agent`).
**Effort:** S (one Origin allowlist function, applied in 3 routes)
**Decision:** fix-later
**Refactor-shape:** Add `assertSameOrigin(req: NextRequest): void` helper in `lib/mcp/session.ts` that throws unless `req.headers.get('origin')` matches `process.env.NEXT_PUBLIC_APP_URL` (or a small allowlist in dev). Call from `POST /api/mcp/reset`, `GET /api/briefing`, `GET /api/agent`. ~15 LOC.

**Why not fix-now:** demo-shape risk today (single-user, no shared session). Becomes load-bearing the moment auth gates multi-user state.

> Cross-ref: `study-security/audit.md` finding B4 + B5. Cleanup audit 2026-06-02 finding #16 — **still live.**

---

### 24. NEW — NDJSON-line reader duplicated across 4 sites (~120 LOC removable)

**Lens:** structural (duplication)
**Severity:** medium — same `getReader() → TextDecoder() → buffer → split('\n') → JSON.parse(line)` loop appears at `app/page.tsx:181-203` (capture path), `app/page.tsx:323-464` (main briefing effect), `lib/hooks/useInvestigation.ts:184-208`, and `components/chat/StreamingResponse.tsx:107`. Four copies that already drift on three real axes: (a) trailing-buffer handling (page.tsx flushes the tail, useInvestigation flushes too, captureAll silently drops it), (b) malformed-line policy (all silently `continue` except useInvestigation which has a slightly different try/catch shape), (c) reader.cancel on unmount (only page.tsx's main effect does it). The prior audit noted ONE half of this as part of #8; the freshly counted count is 4, not 2, which earns its own structural finding.
**Effort:** M (half day — extract `readNdjson<TEvent>(res, onEvent, { onPartial? }): Promise<void>` into `lib/streaming/ndjson.ts`; 4 call sites collapse to ~5 LOC each; add ONE unit test for the buffer-tail and malformed-line behavior the four copies disagreed on)
**Decision:** fix-later
**Refactor-shape:** Create `lib/streaming/ndjson.ts` exporting `async function readNdjson<E>(body: ReadableStream<Uint8Array>, onEvent: (e: E) => void, opts?: { cancelOn?: () => boolean }): Promise<void>`. Replace the four loops with `await readNdjson(res.body, handle)`. Behaviour preserved per site; the unified behavior is a deliberate decision (flush trailing buffer; silently skip malformed lines; cancel when `cancelOn()` returns true) that matches what the page.tsx main effect already does — the *correct* shape that the other three drifted from.

**Why not fix-now:** M effort, and it's a precondition for #8 (page.tsx refactor) — that's the leverage. Don't do #8 before #24; the four-copy drift is what makes #8 unsafe. Pair them in a single session if possible: extract NDJSON parser → write the contract test → then thin the page.

> Cross-ref: `study-frontend-engineering/01-ndjson-stream-reader-hook.md`; `audit-refactor-eval-substrate/02-structural.md`. **NEW in this audit** — promoted from a half-mention inside cleanup-2026-06-02 #8 because re-walking the code at 2026-06-14 turned up 4 sites (not 2 as 06-02 implied).

---

## accept (5 items — document and move on)

### 17. Inline-CSS vs Tailwind drift in components

**Lens:** structural (convention drift)
**Severity:** low — cosmetic; doesn't compound; doesn't block changes. Re-counted at 2026-06-14: `InsightCard.tsx` has 41 inline `style=` attributes, `CoverageGrid.tsx` 38, `EvidencePanel.tsx` 37, `RecommendationCard.tsx` 38 — same convention drift as 06-02.
**Effort:** M (touches many component files; mostly mechanical extraction of repeated style objects into named `CSSProperties` constants)
**Decision:** accept
**Refactor-shape:** Pull repeated style objects in `components/feed/InsightCard.tsx` into named `CSSProperties` constants per component (`cardStyle`, `tileStyle`). The pattern already exists in `components/investigation/EvidencePanel.tsx:13-46`.

**Why accept:** Tailwind v4 landed mid-build and inline `style={{}}` with CSS variables was already the working pattern. A consistent style requires either a top-down style-system decision (out of scope for cleanup) or one-component-at-a-time migration as components are touched for other reasons.

> Cross-ref: `study-software-design/audit.md` red-flag #4. Cleanup audit 2026-06-02 finding #17 — **still live.**

---

### 18. `synthesisInstruction` boilerplate duplicated across 4 agents

**Lens:** structural (partially-pushed-up config)
**Severity:** low — the prefix/suffix duplication is mild; the shape-clause middle is genuinely role-specific.
**Effort:** S (lift `buildSynthesisInstruction(shape: string)` into `lib/agents/base.ts`)
**Decision:** accept
**Refactor-shape:** Add `buildSynthesisInstruction(shape: string): string` to `lib/agents/base.ts`; the four agent classes pass only the shape clause.

**Why accept:** small payoff, no current bug. Wait until either #9 lands (which subsumes the recovery flow) or a fifth agent is added.

> Cross-ref: `study-software-design/audit.md` red-flag #5. Cleanup audit 2026-06-02 finding #18 — **still live.**

---

### 19. `r` and `cp` in `lib/insights/derive.ts`

**Lens:** structural (vague names)
**Severity:** low — cosmetic; two locals; trivially understood from one line of context.
**Effort:** S (two renames)
**Decision:** accept
**Refactor-shape:** `lib/insights/derive.ts:14` rename `r` → `result`; `lib/insights/derive.ts:29` rename `cp` → `period`. Two single-letter rename diffs.

**Why accept:** so trivial it's beneath the noise floor. Fold into the next `derive.ts` touch.

> Cross-ref: `study-software-design/audit.md` red-flag #7. Cleanup audit 2026-06-02 finding #19 — **still live.**

---

### 20. `McpClient.listTools` is a literal pass-through

**Lens:** architectural (layering)
**Severity:** low — earned pass-through, deliberately documented at `lib/mcp/client.ts:165-171`.
**Effort:** N/A
**Decision:** accept
**Refactor-shape:** None — the comment above the method documents the deliberate absence.

**Why accept:** the audit flagged it; the audit also justified it. Recognize, document, move on.

> Cross-ref: `study-software-design/audit.md` red-flag #6. Cleanup audit 2026-06-02 finding #20 — **still live.**

---

### 21. `useInvestigation` doesn't cancel its fetch on cleanup

**Lens:** architectural (effect lifecycle)
**Severity:** low — deliberate; the comment at `lib/hooks/useInvestigation.ts:31-36` explains StrictMode mount-clean-remount aborted the stream.
**Effort:** M (~15 LOC AbortController on Next router events would fix it without re-introducing the StrictMode bug)
**Decision:** accept
**Refactor-shape:** Listen for Next router navigation events and call `controller.abort()` only on navigation, not on effect cleanup. ~15 LOC in the hook.

**Why accept:** wasted-budget cost is small (one Anthropic + ~6 MCP calls per navigate-away — rare event), the deliberate surprise is well-commented, the StrictMode bug class is real, the fix is non-obvious. Park.

> Cross-ref: `study-system-design/audit.md` red-flag #5 (LOW). Cleanup audit 2026-06-02 finding #21 — **still live.**

---

## cannot-clean — leaves cleanup, becomes feature work (1 item)

### 23. No LLM quality eval (Case B)

**Lens:** test (AI quality boundary)
**Severity:** high — every prompt edit and model swap ships with zero quality signal. 144 green Vitest tests prove the wrapper works; they say nothing about model output quality. **Caps the recon ladder at L1.**
**Effort:** L (build `evals/golden/`, `evals/runner.ts`, judge model, regression set; ongoing curation)
**Decision:** cannot-clean — **exits cleanup, becomes feature work.**
**Refactor-shape:** None — this is NOT behaviour-preserving. Building the eval harness IS new behaviour: a runner, a judge model, a goldset of (anomaly → expected diagnosis-class) pairs, a regression set that grows over time, a CI integration. The substrate exists (NDJSON trace + `durationMs` spans replayable via `lib/state/investigations.ts`), but the harness itself is a build.

**Why cannot-clean:** the cleanup audit's strict promise is behaviour-preserving refactors. Building evals is new product surface. Re-file as a phase-of-work item under `study-ai-engineering/05-evals-and-observability/`.

> Cross-ref: `study-testing/audit.md` Top-3 #3; `study-ai-engineering/05-evals-and-observability/`; `recon-2026-06-02.md` Section 1. Cleanup audit 2026-06-02 finding #23 — **still live, scope unchanged.**

---

## Out of scope (by audit construction)

The DSA hot-paths lens (Lens 3) is **not yet exercised** by this codebase. No flat-array → map-needed mismatches, no quadratic-where-linear-possible candidates, no tree/graph traversals where the wrong data structure could bite. Same finding as 06-02; the cleanup pile is concentrated in structural + architectural, not algorithmic.

The test-debt lens (Lens 4) surfaced one fix-later (#15) and the cannot-clean eval gap (#23). The existing 144 tests in `lib/` (re-counted; package.json reports 144) are sharp and well-isolated — no brittle, no lying, no wrong-level findings. The gap is missing tests at the route boundary, not bad tests anywhere.

---

## Triage tally

```
                  count   LOC est.    typical effort
                  ─────   ────────    ──────────────
  fix-now           7      ~110       S each (one afternoon, STILL)
  fix-later        10      ~370       mix S/M/L
  accept            5        —        document, defer
  cannot-clean      1      feature    re-file as new work
                  ─────
  TOTAL            23
```

**Lens distribution of the 22 actionable findings (excluding the cannot-clean):**

```
  lens             fix-now    fix-later    accept
  ──────────       ───────    ─────────    ──────
  structural          3           4          3
  architectural       4           5          2
  dsa                 0           0          0
  test                0           1          0
```

The cluster shape is unchanged: architectural findings dominate (11 of 22 actionable), and within that cluster the split is the same — **observability + failure containment** (#2, #5, #6, plus #4) vs **state-placement + boundaries** (#1, #16, #11, #13). The structural cluster ticked up by one finding because of the freshly counted four-site NDJSON-parser duplication.

---

## Prior-finding cross-reference

| Prior finding (06-02)                          | State at 06-14 | Notes |
|------------------------------------------------|----------------|-------|
| #1  Global-Map insight wipe                    | still-live     | unchanged code; carried as fix-now #1 |
| #2  `res.usage` dropped                        | still-live     | unchanged code; carried as fix-now #2 |
| #3  Four routes leak `e.stack`                 | still-live     | unchanged code; carried as fix-now #3 |
| #4  `/api/mcp/call` no allowlist               | still-live     | unchanged code; carried as fix-now #4 |
| #5  No per-tool MCP timeout                    | still-live     | unchanged code; carried as fix-now #5 |
| #6  No phase-timing on 300s budget             | still-live     | unchanged code; carried as fix-now #6 |
| #7  `e.cause` chain unfiltered                 | still-live     | unchanged code; carried as fix-now #7 |
| #8  page.tsx 817 LOC                           | still-live (+1 useState) | carried as fix-later #8 |
| #9  `synthesize()` duplicated                  | still-live     | carried as fix-later #9 |
| #10 Insight↔Anomaly field-copy                 | still-live     | carried as fix-later #10 |
| #11 No circuit breaker                         | still-live     | carried as fix-later #11 |
| #12 Schema cache no TTL                        | still-live     | carried as fix-later #12 |
| #13 investigations Map monotonic               | still-live     | carried as fix-later #13 |
| #14 AUTH_SECRET no strength                    | still-live     | carried as fix-later #14 |
| #15 No route-level integration tests           | still-live     | carried as fix-later #15 |
| #16 CSRF + GETs-with-side-effects              | still-live     | carried as fix-later #16 |
| #17 Inline-CSS drift                           | still-live     | carried as accept #17 |
| #18 `synthesisInstruction` boilerplate         | still-live     | carried as accept #18 |
| #19 `r` and `cp` vague names                   | still-live     | carried as accept #19 |
| #20 `listTools` pass-through                   | still-live     | carried as accept #20 |
| #21 useInvestigation no cancel                 | still-live     | carried as accept #21 |
| #22 lucide-react possibly-unused               | **resolved**   | confirmed in use at `components/feed/CoverageGrid.tsx:5-17` — drop from list |
| #23 No LLM quality eval                        | still-live     | carried as cannot-clean #23 |
| —                                              | **NEW**        | #24 NDJSON-reader 4-site duplication (re-counted; promoted from #8's body) |

---

## The five-line summary that holds the whole audit

1. **Nothing got done.** All seven 06-02 fix-now items are still live, with the same file:line evidence. That's the most important sentence in this audit.
2. **The single correctness bug is the same correctness bug** — `lib/state/insights.ts:4` global Map, ~30 LOC fix. Do this first.
3. **Three cheap observability + security fixes (#2, #3, #6, #7) total ~14 LOC** — the whole afternoon list could land in one session if you wanted.
4. **One new structural finding promoted** — NDJSON parser duplication is at FOUR sites now, not 2 (re-counted), and it's the blocker for the page.tsx fix-later.
5. **One resolved (lucide-react: now confirmed used)**, **one widened (#8 page.tsx +1 useState)**, and **the eval gap (#23) still exits the cleanup flow** — that's the discipline.
