# Cleanup audit — 2026-06-02

> **Verdict-first.** Most of this codebase's debt is **architectural, not structural** — the units are clean, the seams are tight, the tests are sharp where they exist. The seven items below at `fix-now` cluster on two fronts: (1) one **correctness bug** that fires at ~10 concurrent users (`lib/state/insights.ts:4` global Map), and (2) a small set of **cheap, high-leverage hardening fixes** that the existing study audits already pre-staged with `file:line` and refactor-shape lines. Total fix-now LOC is ~110 across 7 items — one afternoon. The discipline of this audit is that *most findings are not fix-now.* The `app/page.tsx` shallow-module monster (817 LOC) is `fix-soon`, not `fix-now` — it's painful but not actively causing bugs, and L-effort refactors during a non-phase cleanup pass are how behaviour-preserving slides into behaviour-changing. The eval gap (Case B) is `cannot-clean` — it leaves the cleanup flow entirely and becomes feature work.
>
> **Triage shape:** 7 fix-now (3 structural, 4 architectural, 0 dsa, 0 test) · 9 fix-later · 6 accept · 1 cannot-clean. Total: 23 findings.

---

## Top 3 fix-now (the afternoon list)

If you only do three things this week, do these — they are the highest leverage-to-effort ratio in the entire pile, all S effort, all high severity:

1. **Session-key the insights `Map`** — `lib/state/insights.ts:4`, ~30 LOC. Eliminates the only correctness bug at any current scale. Cross-ref: `study-system-design/audit.md` Top-3 #1, `recon-2026-06-02.md` critical correctness bug.
2. **Add `res.usage` logging at the four Anthropic call sites** — `lib/agents/base.ts:102`, `lib/agents/diagnostic.ts:97`, `lib/agents/recommendation.ts:96`, `lib/agents/intent.ts:18`, ~5 LOC. Cheapest fix in the codebase for the most consequential blind spot. Unblocks cost diagnosis on the suspected `synthesize()` cost concentration. Cross-ref: `study-performance-engineering/audit.md` Top-3 #1.
3. **Strip `e.stack` from the four error-response JSON bodies** — `app/api/mcp/call/route.ts:18`, `app/api/mcp/tools/route.ts:19`, `app/api/mcp/tools/check/route.ts:23`, `app/api/mcp/capture/route.ts:55`, ~4 LOC. Cross-ref: `study-security/audit.md` Top-3 #2.

---

## fix-now (7 items)

### 1. Global-Map insight wipe under concurrent users

**Lens:** architectural
**Severity:** high — actively wrong above ~10 concurrent users on a warm Vercel instance; user-visible as feed flicker / investigation 404.
**Effort:** S (under an hour, one file + 3 call sites)
**Decision:** fix-now
**Refactor-shape:** Change `lib/state/insights.ts:4` from `Map<id, Insight>` to `Map<sessionId, Map<id, Insight>>`; thread `sessionId` (already available via `getOrCreateSessionId` in `lib/mcp/session.ts:16`) through `putInsights` / `getInsight` / `listInsights` / `getAnomaly` and the three call sites in `app/api/briefing/route.ts` and `app/api/agent/route.ts`. Per-session sub-map gets `.clear()` on rebriefing, not the global Map. Behaviour preserved per session; the cross-session bleed is the bug being removed.

> Cross-ref: `study-system-design/audit.md` Top-3 #1 + Ceiling 1; `recon-2026-06-02.md` "the critical correctness bug — named but unfixed."

---

### 2. `res.usage` dropped on the floor at every Anthropic call

**Lens:** architectural (observability seam)
**Severity:** high — every prompt edit and model swap ships blind; the dominant cost line (`synthesize()`, finding #5) cannot be sized.
**Effort:** S (5 lines total)
**Decision:** fix-now
**Refactor-shape:** Add `console.log({ site, usage: res.usage, sessionId })` after `anthropic.messages.create` at the four call sites: `lib/agents/base.ts:102`, `lib/agents/diagnostic.ts:97`, `lib/agents/recommendation.ts:96`, `lib/agents/intent.ts:18`. No control-flow change, no API change, no test impact. Behaviour identical; one read of an existing field.

> Cross-ref: `study-performance-engineering/audit.md` Top-3 #1 — "cheapest fix in the codebase for the most consequential blind spot."

---

### 3. Four routes leak `e.stack` in JSON error bodies

**Lens:** structural (consistency drift) + architectural (trust boundary)
**Severity:** high — info disclosure across the route → browser trust boundary; reveals file paths, library entry points, function names to any caller.
**Effort:** S (one line per route, 4 routes)
**Decision:** fix-now
**Refactor-shape:** In `app/api/mcp/call/route.ts:18`, `app/api/mcp/tools/route.ts:19`, `app/api/mcp/tools/check/route.ts:23`, `app/api/mcp/capture/route.ts:55`, change `e.message + '\n' + (e.stack ?? '')` to `e.message`. Keep `console.error(e)` so the operator still sees the stack in Vercel logs. The streaming routes already use this safe shape — the inconsistency is the finding.

> Cross-ref: `study-security/audit.md` Top-3 #2.

---

### 4. `POST /api/mcp/call` accepts any tool name (no allowlist)

**Lens:** architectural (capability gating)
**Severity:** high today by recon rule (gated only by session auth); becomes critical the moment Bloomreach adds a write tool.
**Effort:** S (one Set + one guard, ~10 LOC)
**Decision:** fix-now
**Refactor-shape:** At the top of `app/api/mcp/call/route.ts`, build `const ALL_KNOWN = new Set([...monitoringTools, ...diagnosticTools, ...recommendationTools, ...bootstrapTools])` (imports from `lib/mcp/tools.ts`); after `req.json()`, `if (typeof name !== 'string' || !ALL_KNOWN.has(name)) return NextResponse.json({ error: 'tool not allowed' }, { status: 403 })`. Behaviour preserved for the `/debug` UI that exercises this route (it only calls known tools); the new rejection is the *added* safety, not a behaviour change to existing callers.

> Cross-ref: `study-security/audit.md` Top-3 #1, finding A7/B6. Also see `study-security/05-open-tool-surface-gap.md`.

---

### 5. No per-tool timeout on MCP calls

**Lens:** architectural (failure containment seam)
**Severity:** high — a Bloomreach connection that hangs forever burns the entire 300s route budget on one stuck call; no signal, no recovery.
**Effort:** S (~15 LOC inside `SdkTransport.callTool`)
**Decision:** fix-now
**Refactor-shape:** In `lib/mcp/transport.ts:47–59`, wrap the `this.client.callTool({ name, arguments: args })` call in `Promise.race([..., timeoutPromise(30_000)])` or pass an `AbortSignal` from `AbortSignal.timeout(30_000)` into the SDK if the SDK supports it. Throw `McpToolError(name, 'timeout after 30s')` on timeout. No public API change; `McpClient` already handles thrown errors via the existing retry ladder. The `30_000` ceiling is the same shape as `retryCeilingMs: 20_000` in `lib/mcp/connect.ts` — the rationale lives next to that comment.

> Cross-ref: `study-system-design/audit.md` HIGH finding #2; `recon-2026-06-02.md` "no per-tool timeout."

---

### 6. No phase-timing observability on the 300s budget

**Lens:** architectural (observability)
**Severity:** high — the day a request hits the 300s wall in production, there is no signal which phase ate the time (schema bootstrap / coverage gate / monitoring / diagnostic / recommendation).
**Effort:** S (~20 LOC across two routes)
**Decision:** fix-now
**Refactor-shape:** In `app/api/briefing/route.ts` and `app/api/agent/route.ts`, wrap each major phase with `const t0 = performance.now(); … const elapsed = performance.now() - t0;` and emit `console.log({ route, phase, durationMs: elapsed, sessionId, complete })` on `done`. No API change, no NDJSON wire change (logs are server-only). The `tool_call_end.durationMs` already exists for per-call tracking; this is the per-*phase* layer above it.

> Cross-ref: `study-system-design/audit.md` Top-3 #2; `study-performance-engineering/audit.md` "no Web Vitals / time-to-first-event metric" (the route-side half).

---

### 7. `e.cause` chain logged unfiltered (possible token echo)

**Lens:** structural (one-line filter; logging hygiene)
**Severity:** high if it fires (token in Vercel logs = same as a secret in logs); paired with #3 above as a cluster — both are "what gets logged where."
**Effort:** S (one regex, one wrapper function)
**Decision:** fix-now
**Refactor-shape:** Add a `redactSecrets(text: string): string` helper in `lib/mcp/transport.ts` that strips `Bearer [a-zA-Z0-9._-]+` and `"access_token":\s*"[^"]+"` patterns. Wrap the body capture in `makeCapturingFetch` at `lib/mcp/transport.ts:29` and any `console.error` that prints `err.cause` in the agent path. No control-flow change; the redaction is post-capture.

> Cross-ref: `study-security/audit.md` finding C7; `recon-2026-06-02.md` correlates with the 4-routes-leak-stack cluster.

---

## fix-later (9 items — track but don't act this pass)

### 8. `app/page.tsx` is 817 LOC, 8 concerns, 14 useState slots

**Lens:** structural (shallow module / cognitive load hotspot)
**Severity:** medium — slows future work, makes bugs more likely, but no current correctness issue.
**Effort:** L (a day or more; touches one file but cascades to NDJSON parser shared with `useInvestigation.ts`)
**Decision:** fix-later
**Refactor-shape:** Extract `useBriefingStream(mode)`, `useReconnectPolicy()`, `useDemoCapture(insights, workspace, trace)` into `lib/hooks/`. Page collapses to ~120 LOC of layout + composition. Behaviour preserved if the existing 14 `useState` slots are mapped 1:1 into the three hooks' returned state.

**Why not fix-now:** L-effort refactors during cleanup are the canonical "behaviour-preserving slides into behaviour-changing" failure mode. This deserves its own dedicated session with the existing NDJSON tests pinned as the contract, but the suite *has no NDJSON tests* (see #15) — meaning this refactor would land without a safety net. **Block on #15 (NDJSON harness) first.** That's a feature-work sequencing argument, not a "don't do it" argument.

> Cross-ref: `study-software-design/audit.md` red-flag #1, Top-3 #1; `study-software-design/02-shallow-module-page-component.md` for the deep walk.

---

### 9. `synthesize()` duplicated across two agents (~90 LOC removable)

**Lens:** structural (duplication) + architectural (special case sprawl)
**Severity:** medium — two copies drift independently; same shape, same recovery semantics.
**Effort:** M (half day; modifies `runAgentLoop` signature, removes two methods, updates tests)
**Decision:** fix-later
**Refactor-shape:** Lift `synthesize()` into `runAgentLoop` (`lib/agents/base.ts:48–176`) as `parseResult: (text) => T | null` + `recoveryPrompt: (toolCalls) => string` options. Loop runs as normal, attempts parse, runs ONE tool-less recovery turn on failure. Both `synthesize()` methods in `lib/agents/diagnostic.ts:86–126` and `lib/agents/recommendation.ts:82–132` delete. Existing scripted-Anthropic tests cover the path; the test seam survives.

**Why not fix-now:** medium severity, M effort — it's track-and-batch territory. Touches the most-tested deep module in the codebase (`runAgentLoop`); deserves its own focused session, not a side dish.

> Cross-ref: `study-software-design/audit.md` red-flag #3, Top-3 #3; `study-software-design/04-synthesize-recovery-duplication.md`.

---

### 10. `Insight ↔ Anomaly` field-copy lives in 3 files (silent leak)

**Lens:** structural (information leakage) + architectural (wire format)
**Severity:** medium — TypeScript catches the interface change but not the two copy sites; a new field on `Anomaly` silently drops in the `insightToAnomaly` round-trip.
**Effort:** M (half day; colocation refactor + round-trip test, OR wire-format change)
**Decision:** fix-later
**Refactor-shape:** Cheap version: move `insightToAnomaly` from `app/api/agent/route.ts:29–31` next to `anomalyToInsight` in `lib/state/insights.ts`; add a round-trip test asserting `insightToAnomaly(anomalyToInsight(a))` preserves every field. Deeper version (cannot-clean — see #23): change the wire format so `/api/agent` accepts only the insight id and looks up the cached `Anomaly` server-side. The cheap version is behaviour-preserving; the deeper version changes the wire contract and exits cleanup.

**Why not fix-now:** the cheap version is OK but only patches the symptom (test catches loss after the fact). The real fix requires the wire-format change in #23. Park the cheap version until either (a) a new `Anomaly` field is being added, or (b) #23 lands as feature work.

> Cross-ref: `study-software-design/audit.md` red-flag #2, Top-3 #2; `study-software-design/03-insight-anomaly-silent-leak.md`.

---

### 11. No circuit breaker on the McpClient retry ladder

**Lens:** architectural (failure containment)
**Severity:** medium — during a 5-minute Bloomreach outage at ~10 concurrent users, every request burns 3 retries × ~12s = ~36s before failing; budget evaporates. Fine at 1 user today.
**Effort:** M (~50 LOC; new state on the class, two test files updated)
**Decision:** fix-later
**Refactor-shape:** Add `consecutiveFailures: Map<toolName, number>` + `openUntil: Map<toolName, timestamp>` state to `McpClient`. In the retry handler at `lib/mcp/client.ts:121–132`, after 5 consecutive 5xx for a tool name, set `openUntil = now + 30_000` and fail fast (`McpToolError(name, 'circuit open')`). Close on first success. ~50 LOC, no external dep.

**Why not fix-now:** medium severity at current load (Ceiling 2 in `study-system-design/audit.md` — the ~100x trigger, not ~10x). The session-Map fix (#1) is the ~10x trigger; the circuit breaker is the next horizon. Track behind it.

> Cross-ref: `study-system-design/audit.md` Top-3 #3, Ceiling 2; `study-performance-engineering/audit.md` Top-3 #3 (related: 300s route budget).

---

### 12. Schema cache has no TTL

**Lens:** architectural (caching policy)
**Severity:** medium — brittle the day a customer adds a new event type and a warm instance keeps serving the stale schema for hours.
**Effort:** S (~10 LOC)
**Decision:** fix-later
**Refactor-shape:** In `lib/mcp/schema.ts:131`, change `let cached: WorkspaceSchema | null = null` to `let cached: { value: WorkspaceSchema; expiresAt: number } | null = null` with a 1-hour TTL. Optional `?force=true` query param on the bootstrap path. ~10 LOC.

**Why not fix-now:** the trigger (customer adds event type) doesn't fire today — single-tenant demo against one project. Park until multi-tenant or a customer reports stale schemas.

> Cross-ref: `study-system-design/audit.md` red-flag #4; `study-performance-engineering/audit.md` red-flag #6.

---

### 13. `investigations` Map grows monotonically (no LRU, no delete)

**Lens:** architectural (memory bound)
**Severity:** medium — classic Node leak pattern; instance dies before it matters today, but bites when traffic patterns change.
**Effort:** S (~20 LOC LRU cap)
**Decision:** fix-later
**Refactor-shape:** In `lib/state/investigations.ts:11`, wrap the `mem: Map<string, AgentEvent[]>` in a small LRU with `MAX_INVESTIGATIONS = 100`. On `set`, if size > cap, delete the oldest key. ~20 LOC, no external dep.

**Why not fix-now:** instance recycle bounds it in production today. Add when traffic justifies it or as part of the Vercel KV migration in `study-system-design/audit.md` Ceiling 3.

> Cross-ref: `study-performance-engineering/audit.md` red-flag #5.

---

### 14. `AUTH_SECRET` has no strength enforcement

**Lens:** structural (input validation at app boundary)
**Severity:** medium — `AUTH_SECRET=password` is silently SHA-256'd into a 32-byte AES key with ~10 bits of underlying entropy. The `.env.example` documents `openssl rand -base64 32`; the code doesn't enforce it.
**Effort:** S (one line)
**Decision:** fix-later
**Refactor-shape:** In `lib/mcp/auth.ts:51–60` (`aesKey`), add `if (secret.length < 32) throw new Error('AUTH_SECRET must be at least 32 characters')` before the SHA-256. One line.

**Why not fix-now:** strict gating could break dev environments where the secret hasn't been generated yet. Adding a one-time warning in `console.warn` is the fix-now move if the gating is too aggressive — but most likely the right move is to ship the throw with a clear error message and let the dev experience itself force the fix. Park behind #1–#7 because it's a deployment-prep item, not a code-health item.

> Cross-ref: `study-security/audit.md` Top-3 #3, finding C3.

---

### 15. No route-level integration tests; no NDJSON-stream test

**Lens:** test
**Severity:** medium — load-bearing route (`app/api/agent/route.ts`) has zero tests; every wire between tested lib pieces is unverified. **Blocks safe execution of #8.**
**Effort:** M (extract `runInvestigation(deps)` from the route handler, then apply the existing scripted-Anthropic pattern one layer up)
**Decision:** fix-later
**Refactor-shape:** Extract `runInvestigation(deps: { anthropic, mcp, schemaProvider, sessionId, insight, … })` as a pure async function out of `app/api/agent/route.ts`; leave `GET` as a 5-line wrapper that wires deps from `connectMcp`. Add `test/api/agent.test.ts` using the scripted-Anthropic pattern from `test/agents/base.test.ts`, asserting NDJSON event order + `done` sentinel. Closes test gaps A and C from `study-testing/audit.md`.

**Why not fix-now:** M effort, and it's *enabling* infrastructure rather than fixing a fire. But it's a fix-soon priority because #8 cannot land safely without it.

> Cross-ref: `study-testing/audit.md` Top-3 #1; `study-testing/01-scripted-anthropic-harness.md`.

---

### 16. CSRF on `POST /api/mcp/reset`; GETs with side effects on `/api/briefing` and `/api/agent`

**Lens:** architectural (trust boundary)
**Severity:** medium — `SameSite=None` on the session cookie + no Origin check means a cross-origin form post logs the user out (`reset`) or burns Anthropic tokens (`briefing` / `agent` via `<script src>` or iframe).
**Effort:** S (one Origin allowlist function, applied in 3 routes)
**Decision:** fix-later
**Refactor-shape:** Add `assertSameOrigin(req: NextRequest): void` helper in `lib/mcp/session.ts` that throws unless `req.headers.get('origin')` matches `process.env.NEXT_PUBLIC_APP_URL` (or a small allowlist in dev). Call from `POST /api/mcp/reset`, `GET /api/briefing`, `GET /api/agent`. ~15 LOC.

**Why not fix-now:** demo-shape risk today (single-user, no shared session). Becomes load-bearing the moment auth gates multi-user state.

> Cross-ref: `study-security/audit.md` finding B4 + B5.

---

## accept (6 items — document and move on)

### 17. Inline-CSS vs Tailwind drift in components

**Lens:** structural (convention drift)
**Severity:** low — cosmetic; doesn't compound; doesn't block changes.
**Effort:** M (touches many component files; mostly mechanical extraction of repeated style objects into named `CSSProperties` constants)
**Decision:** accept
**Refactor-shape:** Pull repeated style objects in `components/feed/InsightCard.tsx` (495 LOC, ~150 inline style objects) into named `CSSProperties` constants per component (`cardStyle`, `tileStyle`). The pattern already exists in `components/investigation/EvidencePanel.tsx:13–46`. But the JSX-reads-as-JSX payoff is small relative to the diff size.

**Why accept:** the convention drift exists because Tailwind v4 landed mid-build and inline `style={{}}` with CSS variables was already the working pattern. A consistent style requires either (a) a top-down style-system decision (out of scope for cleanup), or (b) one-component-at-a-time migration as components are touched for other reasons. Don't chase it in a cleanup pass.

> Cross-ref: `study-software-design/audit.md` red-flag #4.

---

### 18. `synthesisInstruction` boilerplate duplicated across 4 agents

**Lens:** structural (partially-pushed-up config)
**Severity:** low — the prefix/suffix duplication is mild; the shape-clause middle is genuinely role-specific.
**Effort:** S (lift `buildSynthesisInstruction(shape: string)` into `lib/agents/base.ts`)
**Decision:** accept
**Refactor-shape:** Add `buildSynthesisInstruction(shape: string): string` to `lib/agents/base.ts`; the four agent classes pass only the shape clause ("JSON array of anomalies", "single JSON object", etc.). The wrapping ("You have NO more tool calls available…" prefix + "Do not say you need more queries" closer) lifts.

**Why accept:** small payoff, touches every agent file, no current bug. Wait until either #9 lands (which restructures the recovery flow anyway and could subsume this) or a fifth agent is added.

> Cross-ref: `study-software-design/audit.md` red-flag #5.

---

### 19. `r` and `cp` in `lib/insights/derive.ts`

**Lens:** structural (vague names)
**Severity:** low — cosmetic; two locals; trivially understood from one line of context.
**Effort:** S (two renames)
**Decision:** accept
**Refactor-shape:** `lib/insights/derive.ts:13` rename `r` → `result`; `:29` rename `cp` → `period`. Two single-letter rename diffs.

**Why accept:** so trivial it's beneath the noise floor. The next time `derive.ts` is touched for any reason, fold these into that diff. Not worth a standalone PR.

> Cross-ref: `study-software-design/audit.md` red-flag #7.

---

### 20. `McpClient.listTools` is a literal pass-through

**Lens:** architectural (layering)
**Severity:** low — earned pass-through, deliberately documented.
**Effort:** N/A (delete the method and have callers reach into `transport.listTools()`)
**Decision:** accept
**Refactor-shape:** None — the comment above the method documents the deliberate absence (the tool list is read once at startup; doesn't count against the rate limit; failures here are fatal anyway). It stays for the "single-import surface for the MCP domain" reason.

**Why accept:** the audit flagged it; the audit also justified it. Recognize, document, move on.

> Cross-ref: `study-software-design/audit.md` red-flag #6.

---

### 21. `useInvestigation` doesn't cancel its fetch on cleanup

**Lens:** architectural (effect lifecycle)
**Severity:** low — deliberate; the comment at `lib/hooks/useInvestigation.ts:31–36` explains StrictMode mount-clean-remount aborted the stream. Cost: a mid-investigation navigate-away wastes Anthropic/MCP budget.
**Effort:** M (~15 LOC AbortController on Next router events would fix it without re-introducing the StrictMode bug)
**Decision:** accept
**Refactor-shape:** Listen for Next router navigation events and call `controller.abort()` only on navigation, not on effect cleanup. ~15 LOC in the hook.

**Why accept:** the wasted-budget cost is small (one Anthropic + ~6 MCP calls per navigate-away — rare event), the deliberate surprise is well-commented, and the StrictMode bug class is real. The fix is non-obvious and risks regressing the documented mount-remount issue. Park.

> Cross-ref: `study-system-design/audit.md` red-flag #5 (LOW).

---

### 22. `lucide-react` possibly-unused dep

**Lens:** structural (dead weight)
**Severity:** low — bundle bloat at most; not load-bearing.
**Effort:** S (one `npm uninstall` if `npx depcheck` confirms unused)
**Decision:** accept
**Refactor-shape:** Run `npx depcheck`; if `lucide-react` confirms unused, `npm uninstall lucide-react`. One-line `package.json` change.

**Why accept:** trivially small; the cost of being wrong (a hidden import) outweighs the payoff. Fold into the next `package.json` touch.

> Cross-ref: `study-security/audit.md` finding E5.

---

## cannot-clean — leaves cleanup, becomes feature work (1 item)

### 23. No LLM quality eval (Case B)

**Lens:** test (AI quality boundary)
**Severity:** high — every prompt edit and model swap ships with zero quality signal. 169 green Vitest tests prove the wrapper works; they say nothing about model output quality. **Caps the recon ladder at L1.**
**Effort:** L (build `evals/golden/`, `evals/runner.ts`, judge model, regression set; ongoing curation)
**Decision:** cannot-clean — **exits cleanup, becomes feature work.**
**Refactor-shape:** None — this is NOT behaviour-preserving. Building the eval harness IS new behaviour: a runner, a judge model, a goldset of (anomaly → expected diagnosis-class) pairs, a regression set that grows over time, a CI integration. The substrate exists (NDJSON trace + `durationMs` spans replayable via `lib/state/investigations.ts`), but the harness itself is a build.

**Why cannot-clean:** the cleanup audit's strict promise is behaviour-preserving refactors. Building evals is new product surface. Re-file as a phase-of-work item under `study-ai-engineering/05-evals-and-observability/` (the design is already drafted next door).

> Cross-ref: `study-testing/audit.md` Top-3 #3; `study-ai-engineering/05-evals-and-observability/`; `recon-2026-06-02.md` Section 1 — the load-bearing gap that caps the LENS verdict.

---

## Out of scope (by audit construction)

The DSA hot-paths lens (Lens 3) is **not yet exercised** by this codebase per `study-dsa-foundations/00-overview.md` — flat-array, hash-map, linear-scan throughout, no quadratic-where-linear-possible candidates, no tree/graph traversals where the wrong data structure could bite. The lens was applied; it found nothing. That's a finding too: the cleanup pile is concentrated in structural and architectural, not algorithmic.

The test-debt lens (Lens 4) surfaced one fix-later (#15) and the cannot-clean eval gap (#23). The existing 169 tests in `lib/` are sharp and well-isolated per `study-testing/audit.md` — no brittle, no lying, no wrong-level findings. The gap is missing tests at the route boundary, not bad tests anywhere.

---

## Triage tally

```
                  count   LOC est.    typical effort
                  ─────   ────────    ──────────────
  fix-now           7      ~110       S each (one afternoon)
  fix-later         9      ~250       mix S/M/L
  accept            6        —        document, defer
  cannot-clean      1      feature    re-file as new work
                  ─────
  TOTAL            23
```

**Lens distribution of the 22 actionable findings (excluding the cannot-clean):**

```
  lens             fix-now    fix-later    accept
  ──────────       ───────    ─────────    ──────
  structural          3           2          3
  architectural       4           6          2
  dsa                 0           0          0
  test                0           1          1
```

The cluster shape: architectural findings dominate (12 of 22), and the architectural cluster splits cleanly between **observability + failure containment** (4 of the 7 fix-now items: #2, #5, #6, plus #4) and **state-placement + boundaries** (#1, #16, #11, #13). The structural cluster is smaller and cosmetic-leaning (one shallow module, two duplication shapes, and a handful of `accept`-grade nits).

The five-line summary that holds the whole audit:

1. **One correctness bug, fix it first** (#1).
2. **Three observability holes that are cheaper to fix than to talk about** (#2, #6, plus the cost-meter half of #2 that unblocks #9).
3. **Three security hardening fixes a reviewer would expect** (#3, #4, #7).
4. **One failure-containment fix that matters before the first production incident** (#5).
5. **Everything else is fix-later, accept, or feature work** — and that's the discipline.
