# Software design audit — 2026-06-14 (action-shaped)

> **Verdict-first.** The 8-lens walk reads the same way the 12-day-old `study-software-design/audit.md` did at the principle level — but the evidence has shifted under it. The `lib/` band is *more* deep-module than it was: `lib/state/insights.ts` went session-keyed (Map<sessionId, SessionFeed>) and `lib/mcp/transport.ts` grew `redactSecrets` + `AbortSignal.timeout` + `isTimeoutError`. Both deep-module verdicts strengthen. The UI band did *not* shift: `app/page.tsx` is still 817 LOC with **14 useState slots** (the prompt's "15" included the `import` line — the actual useState calls are 14) and the same eight concerns at one altitude. The morning's cleanup pass cleared 7 fix-now items; the page.tsx hooks extraction is now blocked only by the NDJSON parser lift, which has its own cleanup stub. The action queue this audit produces is **2 new design stubs + 3 cross-links + 3 documented-only (the gated hook extractions, named but not stubbed because their preconditions aren't met yet)**. Honest: the page.tsx shallow-module finding does not earn 3 design stubs today — the preconditions are an explicit blocker per `cleanup-extract-ndjson-reader.md`. Documenting them with the precondition chain visible is the more useful artifact than emitting 3 stubs that say "execute only after X" three times.
>
> **Triage shape:** 2 new `design-*.md` stubs · 3 cross-links to existing `cleanup-*.md` stubs · 3 documented-without-stub (page.tsx hooks, gated on preconditions) · 5 lens findings unchanged from the 12-day study (cross-linked) · 2 lenses with no new firings.

---

## Where this fits

```
  cleanup-2026-06-14T19-50-14.md       general code-health triage (3 fix-now,
                                       10 fix-later, 5 accept, 1 cannot-clean)
  study-software-design/audit.md       12-day-old comprehension artifact —
                                       same 8 lenses, prose verdicts, no stubs
  design-2026-06-14.md (THIS)          action-shaped — `design-*.md` stubs
                                       for findings not already covered by
                                       a `cleanup-*.md` stub
```

The 3 existing cleanup stubs in `.aipe/specs/refactors/cleanup-*.md` already cover the highest-leverage 3 fix-now items. This audit cross-links to them rather than re-emitting the same work under a `design-` prefix. The new `design-*.md` stubs cover the AOSD-principle findings the cleanup audit either flagged as fix-later or didn't surface at all.

---

## Lens 1 — complexity-in-this-codebase

**Verdict: complexity still has a postal address — the UI/streaming band, `app/page.tsx`.** The 12-day study named change amplification + cognitive load + unknown-unknowns at this file; all three re-fire on re-walk. No new high-severity firings elsewhere.

### Findings

**1.1 — Cognitive-load hotspot at `app/page.tsx:1-817`.** 14 `useState` calls (lines 96–124), 2 `useEffect` blocks (`:129`, `:258`), NDJSON parsing inline at `:181-203` AND `:323-464`, eight concerns at one altitude. Re-counted today. Severity: **high**. Decision: **documented-without-stub** — preconditions block any clean hooks extraction (see Lens 2 finding 2.1 for the full unblocking chain). The page-decomposition notebook (`.aipe/audit-refactor-page-decomposition/00-overview.md`) is the design contract; this audit names the dependency without re-stubbing.

**1.2 — Change-amplification: `Insight ↔ Anomaly` edit-in-three-places.** Field-copy lives in `lib/mcp/types.ts` (interface truth), `lib/state/insights.ts:25-45` (`anomalyToInsight`), `app/api/agent/route.ts:30-32` (`insightToAnomaly`). Same finding as the 12-day study (red-flag #2). Severity: **medium**. Decision: **gets-a-stub** — `design-colocate-insight-anomaly-mappers.md` (logic / module shape). Behaviour-preserving (the silent drop on `evidence/impact/history/category` is a real semantic loss but the *first* refactor — colocation + round-trip test — preserves current shape). The deeper fix (id-only wire) is a feature change and out of scope.

**1.3 — Unknown-unknowns: `filterByStep` reads `AgentEvent` shapes by tag and by nested `agent` field at `app/api/agent/route.ts:67-85`.** Add a new event variant to `lib/mcp/events.ts` and the filter silently drops it from demo replay. Severity: **medium**. Decision: **documented-without-stub** — the fix needs an exhaustiveness check via `satisfies never` or a discriminated-union map, which is a small Replace-Conditional, but the *trigger* is the event-type-addition pattern, not the current shape of `filterByStep`. This is a future-debt finding, not present debt; the cleanup audit (any pass) hasn't surfaced it, and the page-decomposition notebook hasn't either. Naming it here, no stub.

---

## Lens 2 — deep-vs-shallow-modules

**Verdict: the depth axis has stretched at both ends since the 12-day study.** The deep modules got deeper (`lib/mcp/transport.ts` absorbed `redactSecrets` + `AbortSignal.timeout` + `isTimeoutError` per the 06-14 morning commits 9597466 and 56e405b; `lib/state/insights.ts` went session-keyed per c3d47a2). The shallow module did not get shallower — but it did not retreat either. The depth gap is wider.

### Findings

**2.1 — `app/page.tsx` is the shallowest module in the repo (817 LOC, 14 useState, 8 concerns).** Same finding as the 12-day study (red-flag #1) and cleanup `fix-later #4`. The page-decomposition notebook (`.aipe/audit-refactor-page-decomposition/`) sequences five seams (NDJSON kernel → `useModePersistence` → `useBriefingStream` → `useReconnectPolicy` → `useDemoCapture`). Severity: **high** (cognitive load + parser duplication compound).

**Decision: documented-without-stub for the 3 hook extractions.** Honest read of the v1.68.0 spec's "earns a refactor spec" criteria: each hook extraction is behaviour-preserving, specific, localized — they pass. But **two preconditions are not met** (per the page-decomposition notebook's "load-bearing finding") and one of those preconditions (NDJSON kernel extraction) is itself a stub already (`cleanup-extract-ndjson-reader.md`). Emitting 3 hook stubs today would put them in the queue with "execute only after `cleanup-extract-ndjson-reader.md` ships" stamped on each one — that's not a stub, that's a waiting room. The cleaner artifact: name the chain here, cross-link to the precondition stub, defer the 3 hook stubs until the precondition ships. **When `cleanup-extract-ndjson-reader.md` lands, re-run this generator and the 3 hook stubs will emit.**

  - Precondition A (NDJSON kernel extraction) → covered by `.aipe/specs/refactors/cleanup-extract-ndjson-reader.md` ★ **CROSS-LINK**.
  - Precondition B (route-level integration tests for `/api/briefing` + `/api/agent`) → covered by cleanup `fix-later #11`; no stub exists yet because the cleanup audit rated it fix-later.
  - Hook A (`useModePersistence`) — `app/page.tsx:118-150` — easy first lift; ~10 LOC; could ship without precondition A because it doesn't touch the parser. **Documented; no stub today** because the page-decomposition notebook explicitly sequences it AFTER precondition A as the "warm-up" extraction once the integration test seam exists.
  - Hook B (`useBriefingStream`) — `app/page.tsx:258-476` — main briefing effect; depends on precondition A.
  - Hook C (`useReconnectPolicy`) — `app/page.tsx:400-435` — reconnect-on-revoked-token branch buried inside NDJSON `error` handler; depends on precondition A + B (the reconnect path is the load-bearing thing precondition B's tests have to catch).
  - Hook D (`useDemoCapture`) — `app/page.tsx:156-256` — `postCapture` + `runInvestigation` + `captureAll`; depends on precondition A.

> Cross-ref: `.aipe/study-software-design/02-shallow-module-page-component.md` (12-day comprehension walk). `.aipe/audit-refactor-page-decomposition/00-overview.md` (5-seam reframing). Cleanup `fix-later #4`.

**2.2 — Deep modules got deeper (praise + verdict-shift).** `McpClient` (`lib/mcp/client.ts`) still hides retry / cache / spacing / error tagging behind 3 methods; `runAgentLoop` (`lib/agents/base.ts:48-176`) still hides the Anthropic tool-use loop. **New depth:** `lib/mcp/transport.ts` now hides per-tool timeout (`AbortSignal.timeout(TOOL_TIMEOUT_MS)` at `:102` and `:121`), timeout-error normalization (`isTimeoutError` at `:36`), and secret redaction (`redactSecrets` at `:58`) — three things absorbed since the 12-day study. The transport boundary went from "deep" to "deeper." No design action; just a re-verdict.

**2.3 — `lib/state/insights.ts` went from shallow-to-medium to medium-to-deep.** The 12-day study didn't flag this file because it was small and obvious. The 06-14 morning commit c3d47a2 reshaped it into a session-keyed `Map<sessionId, SessionFeed>` with three sub-maps (insights / investigations / anomalies). The interface stayed narrow (~8 functions, each `(sessionId, …)`); the body absorbed the multi-tenancy concern that warm-Vercel serving multiple users had silently violated before. **The right hide:** the test for "did the boundary hide what it should?" passes — every caller's signature gained one parameter (`sessionId`); zero callers reach into the Map<Map<>> structure. Praise; no design action.

---

## Lens 3 — information-hiding-and-leakage

**Verdict: strong hides survived, one severe leak survived, one new leak surfaced.** The 12-day study named the Insight↔Anomaly leak (Lens 1 finding 1.2 above) and the `synthesize()` duplication (Lens 6 finding 6.1 below). Both still fire. **One new finding:** `formatError` is now duplicated across 2 route files and about to be duplicated to 6 if `cleanup-redact-secrets-mcp-routes.md` ships without lifting it first — but that stub's Step 1 already covers the lift, so this is a **cross-link**.

### Findings

**3.1 — `Insight ↔ Anomaly` silent-drop across 3 files.** See Lens 1 finding 1.2 above. **Gets a stub:** `design-colocate-insight-anomaly-mappers.md`.

**3.2 — `formatError` duplicated in 2 routes, about to clone to 6.** `app/api/agent/route.ts:110` and `app/api/briefing/route.ts:80` are byte-identical. The four short MCP routes don't have it yet — `cleanup-redact-secrets-mcp-routes.md` Step 1 lifts the helper into `lib/mcp/transport.ts` next to `redactSecrets` BEFORE Step 2 extends the safe-shape to the four MCP routes, so the duplication caps at 1 file. **CROSS-LINK** — finding owned by the existing cleanup stub; no new design stub.

> Cross-ref: `.aipe/specs/refactors/cleanup-redact-secrets-mcp-routes.md` Step 1.

**3.3 — Strong hides (praise, no action):**
- `parseRetryAfterMs` (`lib/mcp/client.ts:31-38`) — owns the Bloomreach rate-limit error grammar. One file matches `grep retry-after`. ✓
- `redactSecrets` (`lib/mcp/transport.ts:58`) — owns the secret-pattern surface. One file matches `grep -E "token|password|secret|key|authorization|api[_-]?key"` regex sources. ✓ **New since the 12-day study.**
- `BloomreachAuthProvider` (`lib/mcp/auth.ts:33-143`) — owns the three-backend selection by `NODE_ENV`. ✓
- `lib/state/insights.ts` — owns the session-tenancy boundary. ✓ **Newly load-bearing since c3d47a2.**

---

## Lens 4 — layers-and-abstractions

**Verdict: layers transform almost everywhere; one earned pass-through.** Unchanged from the 12-day study.

### Findings

**4.1 — `McpClient.listTools` is a literal pass-through** (`lib/mcp/client.ts:168-171`). The comment above the method documents the deliberate absence (tool list read once at startup, doesn't count against rate limit, failures here are fatal). **Documented-without-stub.** Severity: **low**. Cleanup audit rated this `accept #16` for the same reason. No design action.

**4.2 — Four agent classes share constructor-plus-one-method shape (soft pass-through-adjacent).** Same as 12-day study. Judge call: keep as classes (constructor-once beats functions-with-shared-options-bag at this size). No design action.

---

## Lens 5 — complexity / pull-complexity-downward

**Verdict: mostly healthy; one finding pre-existed and rides along with `synthesize()` duplication.**

### Findings

**5.1 — `synthesisInstruction` boilerplate ×4.** Four agent classes — `MonitoringAgent` (`lib/agents/monitoring.ts:102`), `DiagnosticAgent` (`lib/agents/diagnostic.ts:63`), `RecommendationAgent` (`lib/agents/recommendation.ts:58`), `QueryAgent` (`lib/agents/query.ts:42`) — share a prefix ("You have NO more tool calls available...") and a closer ("Do not say you need more queries"); the shape-clause middle is genuinely role-specific. **Gets a stub:** `design-lift-synthesis-instruction-builder.md`. Severity: **medium** (cleanup `fix-later #13` — promoted from accept this morning specifically to ride along with the `synthesize()` lift; this audit emits the design stub for it).

Same finding as 12-day study red-flag #5. **Rides along with finding 6.1** — both edit `lib/agents/base.ts` and the 4 agent classes, both are part of the "lift agent boilerplate into the loop" through-line.

---

## Lens 6 — classitis / pass-through methods / errors-and-special-cases

**Verdict: strongest design facet in the codebase — three intentional error boundaries (`McpClient` masks rate-limit, `runAgentLoop` masks per-tool failure, agent classes mask parse-fail with FALLBACK). One special-case duplication still fires.**

### Findings

**6.1 — `synthesize()` duplicated across `DiagnosticAgent` and `RecommendationAgent`.** `lib/agents/diagnostic.ts:87-126` and `lib/agents/recommendation.ts:82-132`. Same shape — serialize tool-call history, run a tool-less Anthropic call with recovery prompt, parse result, return null on failure. Same finding as 12-day study red-flag #3 and cleanup `fix-later #5`. Severity: **medium-high** (two ~50-line copies of the same special case).

**Gets a stub:** `design-lift-synthesize-into-runagentloop.md` — define-it-out as `parseResult: (text) => T | null` + `recoveryPrompt: (toolCalls) => string` options on `runAgentLoop`. Both `synthesize()` methods delete (~90 LOC removed). Rides with finding 5.1 (synthesisInstruction lift) — same files touched, same through-line. Two stubs, one execution session if the executor wants to pair them, but kept separate per the "one technique per spec" discipline in `refactor.md`.

**6.2 — No new classitis. No try/catch sprawl.** Praise carried forward unchanged from 12-day study.

---

## Lens 7 — readability (names · comments · consistency · obviousness)

**Verdict: strong overall; one consistency drift (inline-CSS vs Tailwind) and two vague names. All carried forward from the 12-day study and the cleanup audit; no new findings on re-walk.**

### Findings

**7.1 — `r` and `cp` in `lib/insights/derive.ts:13` and `:29`.** Cleanup `accept #15`. **Documented-without-stub** — below the noise floor; fold into next `derive.ts` touch. Severity: **low**.

**7.2 — Inline-CSS vs Tailwind drift in `components/feed/InsightCard.tsx` (~150 inline style objects, 495 LOC).** Cleanup `accept #14`. **Documented-without-stub** — convention drift exists because Tailwind v4 landed mid-build; migrate one component at a time. Severity: **low**. A `design-visual-*.md` stub here would be the right shape if a wholesale conversion ever became the plan — today's call is component-at-a-time as the components get touched.

---

## Lens 8 — red-flags-audit (capstone)

The 12 Ousterhout red flags, re-scored against the current evidence:

```
FIRES (ranked)
  #1  shallow module                CRITICAL  app/page.tsx (still 817 LOC,
                                              14 useState, 8 concerns)
                                              → documented-without-stub (Lens 2.1)
                                                preconditions block stubs today
  #2  information leakage           HIGH      Insight↔Anomaly field-copy ×3
                                              → design-colocate-insight-anomaly-mappers.md
  #3  special-case sprawl           HIGH      synthesize() duplicated ×2
                                              → design-lift-synthesize-into-runagentloop.md
  #4  partially-pushed-up config    MEDIUM    synthesisInstruction boilerplate ×4
                                              → design-lift-synthesis-instruction-builder.md
  #5  duplication (cross-file)      MEDIUM    NDJSON parser ×4
                                              → CROSS-LINK: cleanup-extract-ndjson-reader.md
  #6  duplication (mild)            LOW       formatError ×2 (about to be ×6)
                                              → CROSS-LINK: cleanup-redact-secrets-mcp-routes.md Step 1
  #7  convention drift              MEDIUM    inline-CSS vs Tailwind
                                              → documented-without-stub (Lens 7.2)
  #8  pass-through method           LOW       McpClient.listTools (earned — keep)
                                              → documented-without-stub (Lens 4.1)
  #9  vague names                   LOW       r, cp in lib/insights/derive.ts
                                              → documented-without-stub (Lens 7.1)

DOESN'T FIRE (praise)
  - classitis              4 agent classes earn their keep (Lens 6)
  - try/catch sprawl       errors masked at 3 intentional boundaries (Lens 6)
  - comment restates code  comments load-bearing throughout lib/
  - hard-to-read names     >95% of names are precise
  - temporal decomposition the synthesize() duplication is mild instance only
  - exposed-knob sprawl    knobs well-defaulted; one earned per-call knob
```

5 of 9 fires still concentrate in two places — `app/page.tsx` (#1, contributes to #5 + #7) and the agent classes (#3, #4). That's the same clustering the 12-day study named, surviving 14 days and a 7-commit cleanup sweep.

---

## Top 3 highest-leverage stubs to execute first

Of the 2 design stubs this audit emits + the 3 cross-linked cleanup stubs, the leverage ranking is:

```
  rank   stub                                          leverage
  ────   ────────────────────────────────────────────  ────────
  1      cleanup-extract-ndjson-reader.md ★            high — unblocks 3 hook
         (CROSS-LINK)                                  extractions on page.tsx,
                                                       caps NDJSON parser drift,
                                                       enables one contract test
  2      design-lift-synthesize-into-runagentloop.md   medium-high — retires 2
                                                       ~50-LOC copies, removes a
                                                       special case from 2 agents,
                                                       enables 5.1 to ride along
  3      design-colocate-insight-anomaly-mappers.md    medium — closes the worst
                                                       information leak in the
                                                       codebase, sets the stage
                                                       for the deeper id-only
                                                       wire format if that ever
                                                       becomes the plan
```

The cleanup stubs at rank 1 are already queued; rank 2 + 3 are the new design contracts this audit adds.

---

## Tally

```
                              count   notes
                              ─────   ─────────────────────────────────────
  new design-*.md stubs         2     1 logic (insight mappers), 1 logic
                                      (synthesize lift). 0 frontend, 0 visual
                                      stubs emit today.
  ride-along design-*.md stub   1     synthesisInstruction lift — pairs with
                                      the synthesize() lift; emitted because
                                      cleanup fix-later #13 promoted it.
  cross-links to cleanup-*.md   3     NDJSON parser extraction (Lens 5/2.1
                                      precondition), formatError lift (Lens 3.2),
                                      sessionId threading (not a design finding,
                                      cross-linked for completeness)
  documented-without-stub       7     page.tsx shallow module + 3 hook
                                      extractions (blocked) + listTools earned
                                      pass-through + r/cp vague names + inline-
                                      CSS drift + filterByStep unknown-unknowns
  ─────                         ─────
  TOTAL findings handled       13     across 8 lenses
```

```
  template routing of the 3 new stubs:
    refactor.md → design-<name>.md        ×3  (all logic findings; agent layer)
    refactor-frontend-behaviour.md         ×0  (the 4 hook extractions would
                                               have used this template — held
                                               for the next run after
                                               cleanup-extract-ndjson-reader
                                               ships)
    refactor-frontend-visual.md            ×0  (inline-CSS drift was the only
                                               visual candidate; rated below
                                               the threshold per cleanup audit)
```

---

## Honest verdict on the page.tsx hook extractions

**They were documented in this summary; no `design-frontend-*.md` stubs emitted today.** The call comes from the v1.68.0 spec's "what earns a refactor spec" gate: behaviour-preserving (yes), specific (yes, the page-decomposition notebook names exact line ranges), localized (yes, three named hooks plus one warm-up). All three criteria pass. **But** the audit also calls for honest deduping against existing cleanup material: precondition A is `cleanup-extract-ndjson-reader.md`, already a stub, already queued. Emitting 3 frontend stubs today would each carry a "blocked on cleanup-extract-ndjson-reader.md" line in their Why field — that's a queueing artifact, not a refactor contract.

The right shape: name the chain in this summary (Lens 2 finding 2.1), cross-link to the precondition stub, defer the 3 hook stubs to the next run of `/aipe:audit-software-design` after the precondition ships. When it does, the next run will (a) verify the parser is single-sourced, (b) verify the integration-test seam exists (precondition B), and (c) emit `design-frontend-extract-useBriefingStream.md`, `design-frontend-extract-useReconnectPolicy.md`, `design-frontend-extract-useDemoCapture.md` (plus optionally `design-frontend-extract-useModePersistence.md` as the warm-up per the page-decomposition notebook's 5-seam reframing).

This is the difference between **the audit emits a stub to record what's known** and **the audit emits a stub to defer the work**. The former is signal; the latter is noise. Today's audit takes the signal path.

---

## What to do next (single next action)

Review the 2 new `design-*.md` stubs + 1 ride-along stub. Pick the highest-leverage one — `design-lift-synthesize-into-runagentloop.md` if the goal is "retire the worst special-case duplication," `design-colocate-insight-anomaly-mappers.md` if the goal is "close the worst information leak." Hand the chosen stub to a separate session to execute.

Then ship `cleanup-extract-ndjson-reader.md`. Then re-run this generator — the page.tsx hook extractions will emit.
